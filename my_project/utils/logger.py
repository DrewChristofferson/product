from ..scraper.utils import write_to_txt

def log(file_path, data):
    # for the moment we are just printing the data, but in the future we can output logs to a file
    # write_to_txt(file_path, data)
    print(data)
